document.addEventListener('DOMContentLoaded', () => {
    console.log('Family Dentistry Dearborn site loaded.');

    // Smooth scrolling is handled by CSS, but we can add active state for nav links or mobile menu toggle here later.

    // Example: Simple mobile menu toggle logic if we add a hamburger icon
    /*
    const menuToggle = document.querySelector('.menu-toggle');
    const navList = document.querySelector('.nav-list');
    
    if (menuToggle) {
        menuToggle.addEventListener('click', () => {
            navList.classList.toggle('active');
        });
    }
    */
});
